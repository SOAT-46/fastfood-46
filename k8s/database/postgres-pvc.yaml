apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fastfood-db-pvc
  labels:
    app: fastfood-db
    tier: database
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
