apiVersion: v1
kind: Service
metadata:
  name: pizzaria-api
  labels:
    app: pizzaria-api
    tier: backend
spec:
  type: NodePort
  selector:
    app: pizzaria-api
    tier: backend
  ports:
  - protocol: TCP
    port: 3000 # Service port (exposed to the app)
    targetPort: 3000 # Container port (API port inside the pod)
    nodePort: 30163
